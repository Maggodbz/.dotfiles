preload = ~/Repos/.dotfiles/assets/images/dbz_background.jpg

# Empty monitor field = every output, including ones that appear later when
# docking. Naming a specific monitor breaks silently as soon as that name
# changes (this was pinned to a non-existent "eDP-2").
wallpaper = ,~/Repos/.dotfiles/assets/images/dbz_background.jpg
